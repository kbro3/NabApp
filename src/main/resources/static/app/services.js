angular.module('nabApp.services', []).factory('Customer', function($resource) {
  return $resource('/customers/:id', { id: '@id' }, {
    update: {
      method: 'PUT'
    }
  });
}).service('popupService',function($window){
    this.showPopup=function(message){
        return $window.confirm(message);
    }
});


